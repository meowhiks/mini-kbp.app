# HTTPS mirrors of HTTP vhosts (local / hosts → 127.0.0.1:443).
# Cloudflare Tunnel continues to hit :80 over plain HTTP.

<VirtualHost *:443>
    ServerName lk.mini-kbp.site
    ServerAlias localhost.mini-kbp.site

    Include conf/extra/minikbp-ssl.conf
    Include conf/extra/minikbp-common.conf
    Include conf/extra/minikbp-csp.conf

    ProxyPass /v0/ http://api:8000/v0/ responsefieldsize=65536
    ProxyPassReverse /v0/ http://api:8000/v0/

    <Location /v0/journal-presence/stream/>
        ProxyPass http://api:8000/v0/journal-presence/stream/
        SetEnv proxy-nokeepalive 1
        SetEnv proxy-sendchunked 1
        SetEnv no-gzip 1
    </Location>

    RewriteEngine On

    RewriteRule ^/staff$ https://panel.mini-kbp.site/ [R=301,L,NE]
    RewriteRule ^/staff/(.*)$ https://panel.mini-kbp.site/$1 [R=301,L,NE]

    ProxyPass /admin/ !
    RedirectMatch 404 ^/admin(/.*)?$

    <Location />
        ProxyErrorOverride On
    </Location>
    <Location /v0/>
        ProxyErrorOverride Off
    </Location>

    RewriteCond %{REQUEST_URI} !^/v0/
    RewriteCond %{REQUEST_URI} !^/errors(/.*)?$
    RewriteCond %{HTTP:Upgrade} =websocket [NC]
    RewriteRule /(.*) ws://web:3000/$1 [P,L]

    RewriteCond %{REQUEST_URI} !^/v0/
    RewriteCond %{REQUEST_URI} !^/errors(/.*)?$
    RewriteCond %{HTTP:Upgrade} !=websocket [NC]
    RewriteRule /(.*) http://web:3000/$1 [P,L]

    ProxyPassReverse / http://web:3000/

    ErrorLog /proc/self/fd/2
    CustomLog /proc/self/fd/1 combined
</VirtualHost>

<VirtualHost *:443>
    ServerName panel.mini-kbp.site

    Include conf/extra/minikbp-ssl.conf
    Include conf/extra/minikbp-common.conf
    Include conf/extra/minikbp-csp.conf

    ProxyPass /v0/ http://api:8000/v0/ responsefieldsize=65536
    ProxyPassReverse /v0/ http://api:8000/v0/

    <Location /v0/journal-presence/stream/>
        ProxyPass http://api:8000/v0/journal-presence/stream/
        SetEnv proxy-nokeepalive 1
        SetEnv proxy-sendchunked 1
        SetEnv no-gzip 1
    </Location>

    RewriteEngine On

    <LocationMatch "^/_next/">
        Header always set Cache-Control "no-store"
        Header always set CDN-Cache-Control "no-store"
        Header always set Cloudflare-CDN-Cache-Control "no-store"
    </LocationMatch>

    ProxyPass /admin/ !
    RedirectMatch 404 ^/admin(/.*)?$

    <Location />
        ProxyErrorOverride On
    </Location>
    <Location /v0/>
        ProxyErrorOverride Off
    </Location>

    RewriteCond %{REQUEST_URI} !^/v0/
    RewriteCond %{REQUEST_URI} !^/errors(/.*)?$
    RewriteCond %{HTTP:Upgrade} =websocket [NC]
    RewriteRule /(.*) ws://web:3000/$1 [P,L]

    RewriteCond %{REQUEST_URI} !^/v0/
    RewriteCond %{REQUEST_URI} !^/errors(/.*)?$
    RewriteCond %{HTTP:Upgrade} !=websocket [NC]
    RewriteRule /(.*) http://web:3000/$1 [P,L]

    ProxyPassReverse / http://web:3000/

    ErrorLog /proc/self/fd/2
    CustomLog /proc/self/fd/1 combined
</VirtualHost>

<VirtualHost *:443>
    ServerName mini-kbp.site
    ServerAlias www.mini-kbp.site

    Include conf/extra/minikbp-ssl.conf
    Include conf/extra/minikbp-common.conf
    Include conf/extra/minikbp-csp.conf

    ProxyPass /v0/ http://api:8000/v0/ responsefieldsize=65536
    ProxyPassReverse /v0/ http://api:8000/v0/

    RewriteEngine On

    RewriteRule ^/staff$ https://panel.mini-kbp.site/ [R=301,L,NE]
    RewriteRule ^/staff/(.*)$ https://panel.mini-kbp.site/$1 [R=301,L,NE]

    ProxyPass /admin/ !
    RedirectMatch 404 ^/admin(/.*)?$

    <Location />
        ProxyErrorOverride On
    </Location>
    <Location /v0/>
        ProxyErrorOverride Off
    </Location>

    RewriteCond %{REQUEST_URI} !^/v0/
    RewriteCond %{REQUEST_URI} !^/errors(/.*)?$
    RewriteCond %{HTTP:Upgrade} =websocket [NC]
    RewriteRule /(.*) ws://web:3000/$1 [P,L]

    RewriteCond %{REQUEST_URI} !^/v0/
    RewriteCond %{REQUEST_URI} !^/errors(/.*)?$
    RewriteCond %{HTTP:Upgrade} !=websocket [NC]
    RewriteRule /(.*) http://web:3000/$1 [P,L]

    ProxyPassReverse / http://web:3000/

    ErrorLog /proc/self/fd/2
    CustomLog /proc/self/fd/1 combined
</VirtualHost>

<VirtualHost *:443>
    ServerName api.mini-kbp.site

    Include conf/extra/minikbp-ssl.conf
    Include conf/extra/minikbp-common.conf

    RewriteEngine On

    ProxyPass /admin/ !
    RedirectMatch 404 ^/admin(/.*)?$

    ProxyPass / http://api:8000/ responsefieldsize=65536
    ProxyPassReverse / http://api:8000/

    ErrorLog /proc/self/fd/2
    CustomLog /proc/self/fd/1 combined
</VirtualHost>

# Production API: код в образе, без bind-mount server/
FROM python:3.12-slim

WORKDIR /app

COPY server/requirements.txt .
RUN --mount=type=cache,target=/root/.cache/pip \
    pip install -r requirements.txt

COPY server/ .

EXPOSE 8000

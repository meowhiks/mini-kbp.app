# Copy to android/keystore.properties and fill passwords (file is gitignored).
# storeFile is relative to the android/ directory.
storeFile=../key
storePassword=YOUR_PASSWORD
keyAlias=key0
keyPassword=YOUR_PASSWORD
